'use strict';
/* exported getIndexes */
// getIndexes(array)
function getIndexes(array) {
  const indexes = [];
  for (let i = 0; i < array.length; i++) {
    indexes.push(i);
  }
  return indexes;
}
